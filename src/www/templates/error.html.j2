<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>mapot</title>
    <meta name='description' content="A page showing MAC addresses presence over time">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/png" href="/static/img/favicon.png">

    <link rel="stylesheet" href="/static/css/bootstrap-4.3.1.min.css">
    <style>
    .huge { font-size: 10em;}
    .big { font-size: 5em; color: grey;}
    </style>
  </head>
  <body>
    <div class="container">
      <header class="mt-3">
        <h1 class="display-4">An error occured</h1>
      </header>
      <p><span class="huge">{{ error.code }}</span>&nbsp;&nbsp;&nbsp;<br class="d-inline d-sm-inline d-md-none"><span class="big">{{ error.name }}</span></p>
      <p>{{ error.description }}</p>
    </div>
  </body>
</html>
